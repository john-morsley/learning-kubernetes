---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: example
  name: example-ui-deployment
spec:
  selector:
    matchLabels:
      app: example-ui
  replicas: 1
  template:
    metadata:
      labels:
        app: example-ui
    spec:
      containers:
      - name: example-ui-pod
        image: johnmorsley/example-ui:latest
        env:
        - name: API_ENDPOINT
          value: http://example-api-service.example.svc.cluster.local:5080
        #envFrom:
        #  - configMapRef:
        #      name: globo-identity-config
        #  - secretRef:
        #      name: globoconn-secrets 
        #livenessProbe:
        #  httpGet:
        #    path: /.well-known/openid-configuration
        #    port: 5000
        #  initialDelaySeconds: 20
        #  timeoutSeconds: 5
        #  periodSeconds: 10
        #  failureThreshold: 3
        #readinessProbe:
        #  httpGet:
        #    path: /.well-known/openid-configuration
        #    port: 5000
        #  initialDelaySeconds: 20
        #  timeoutSeconds: 5
        #  periodSeconds: 10
        #  failureThreshold: 3
        #resources:
        #  limits:
        #    memory: "128Mi"
        #    cpu: "200m"
        ports:
        - containerPort: 80
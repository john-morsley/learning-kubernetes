---
apiVersion: v1
kind: Service
metadata:
  namespace: example
  name: example-ui-service
spec:
  selector:
    app: example-ui
  type: ClusterIP
  ports:
    - protocol: TCP
      name: http
      targetPort: 80
      port: 6080